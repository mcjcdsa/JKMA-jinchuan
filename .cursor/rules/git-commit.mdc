---
description: 主要代码变更后及时 Git 提交与推送
alwaysApply: true
---

# Git 提交习惯

- 在完成**主要代码或内容变更**（功能、修复、文案、配置、样式等）后，应**主动询问用户**是否需要提交；若用户希望「有改就提交」，则**在当次任务末尾**执行 `git add`（仅包含本次相关文件）、`git commit`（清晰英文或中文说明）、`git push`。
- **不要**将无关的大文件、`node_modules`、本地机器人目录等一并提交，除非用户明确要求。
- 提交前可用 `git status` 确认变更范围。
